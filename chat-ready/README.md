# Chat-drop files

这里的 Markdown 文件是对应 Skill 的单文件投喂版。选择一个文件，下载后直接拖进支持文件附件的聊天框，再发送：

```text
请把附件当作本次任务的视觉工作规范，不要复述，直接按它出图。
目标：<主题>；比例：<平台/比例>；必须保留：<内容>；禁止出现：<内容>。
先返回 test 图。
```

文件内容与 skills/<skill-name>/SKILL.md 保持一致；正式安装和长期复用仍以 skills/ 目录为准。
